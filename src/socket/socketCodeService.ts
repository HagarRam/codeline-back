// import { Server, Socket } from 'socket.io';
// import { io } from '../app';

// io.on('conncetion', (socket) => {
// 	console.log(`conncet to ${socket}`);
// 	socket.on('send_message', (data) => {
// 		socket.join(socket.id);
// 		console.log(`the data ${data}`);
// 	});
// });
